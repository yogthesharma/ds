var multiply = function (num1, num2) {
  num1 = num1 * 1;
  num2 = num2 * 1;

  const res = `${num1 * num2}`;
  return res;
};

console.log(multiply("3", "4"));

// this is not as simple as it looks
// keep trying to understand this problem
